# ============================================================================
# HybridCreditLLM — Core Dependencies
# ============================================================================
# Pin major versions for reproducibility across local + Kaggle environments.
# GPU-specific libraries (torch, transformers, etc.) are installed separately
# on Kaggle notebooks since they come pre-installed.
# ============================================================================

# ── Data & Core ──────────────────────────────────────────────
pandas>=2.0,<3.0
numpy>=1.24,<2.0
polars>=0.20,<1.0
pyarrow>=14.0

# ── Classical ML ─────────────────────────────────────────────
scikit-learn>=1.3,<2.0
lightgbm>=4.0,<5.0
xgboost>=2.0,<3.0
optuna>=3.4,<4.0
imbalanced-learn>=0.11,<1.0       # SMOTETomek

# ── Visualization ───────────────────────────────────────────
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
plotly>=5.18,<6.0

# ── Explainability & Fairness ───────────────────────────────
shap>=0.43,<1.0
dice-ml>=0.11,<1.0
aequitas>=1.0,<2.0

# ── NLP / Text Processing ───────────────────────────────────
nltk>=3.8,<4.0
beautifulsoup4>=4.12,<5.0

# ── Config & Utilities ──────────────────────────────────────
pyyaml>=6.0,<7.0
tqdm>=4.66,<5.0
joblib>=1.3,<2.0

# ── SEC EDGAR ────────────────────────────────────────────────
requests>=2.31,<3.0
lxml>=5.0,<6.0

# ============================================================================
# GPU-ONLY (install on Kaggle/Colab, not needed locally for CPU tasks):
# pip install torch transformers peft bitsandbytes accelerate unsloth
# ============================================================================
